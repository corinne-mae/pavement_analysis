from src.data_processing import load_data, split_data, process_features


if __name__ == "__main__":
    # Example usage:
    # df = load_data("data/raw/your_data.csv")
    # train_df, test_df = split_data(df, test_size=0.2)
    # X_processed, y_processed, preprocessor = process_features(...)
    print("Functions imported successfully!")
    